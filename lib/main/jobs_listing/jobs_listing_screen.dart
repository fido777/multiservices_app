import 'package:flutter/material.dart';

class JobsListingScreen extends StatelessWidget {
  const JobsListingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Jobs Listing Screen"),);
  }
}